reset
#set xr < range(@xrange) %>
#set yr < range(@yrange) %>
#set ylabel "< @ylabel %>"
#set xlabel "< @xlabel %>"
#set nokey
#set notitle
#set size 0.6,0.6
#set term postscript eps mono
#set out "<%= @output %>"

#< @functions.each do |f| %>
#  < define(f) %>
#< end %>

#plot < @plots.each do |plot| %> \
#       < plot_line(plot) %>, \
#     < end %>
plot x
